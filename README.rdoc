== HOGWARTS from Hannah!

Hogwarts is open for business

To run:

Clone the repository

Then run the following commands:
bundle install
rake db:create
rake db:migrate
rake db:seed
rails server

Open your browser to localhost:3000 and have fun!
